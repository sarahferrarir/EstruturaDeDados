#include <iostream>
#include <cstdio>

using namespace std;

int main(){
    float a, b, c, d;
    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;

    printf("DIFERENCA = %.1lf\n", ((a * b) - (c * d)));

    return 0;
}