#include <iostream>
#include <cstdio>

using namespace std;

int main(){
    float a, b, c, d;
    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;

    printf("diferenca + %;01f\n", ((a * b) - (c * d)));
}